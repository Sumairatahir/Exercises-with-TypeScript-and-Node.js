//q 36T-Shirt: Write a function called make_shirt() that accepts a size and the text of a message that should
// be printed on the shirt. The function should print a sentence summarizing the size of the shirt and
// the message printed on it. Call the function. 

//FUNCTION:

let make_shirt:{
    size:string;
    message:string;
}
make_shirt={
    size:"medium",
    message:"hello world",
 }
console.log(make_shirt);
let user:{
    size:string;
    
    message:string;
} ;
user = {
    size:"medium",
    
    message:"i m muslim",
};
console.log(user)